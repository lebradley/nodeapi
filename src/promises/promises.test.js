const expect = require('chai').expect;
const promises = require('../src/promises');

it('that the promise has a resolve and reject', () => {
    expect(1 == 1).to.equal(true);
});

it('intercept that const... then can make the promise pass or fail', () => {
    promise()
});